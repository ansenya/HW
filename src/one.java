import java.util.Scanner;

public class one {

    public static double mult(int num, int den){
        Fraction fraction = new Fraction(num, den);
        double a = Fraction.numerator;
        double b = Fraction.denominator;
        return 0;
    }

    public static double multa(int num, int den){
        Fraction fraction = new Fraction(num, den);
        double a = Fraction.numerator;
        return a;
    }
    public static double multb(int num, int den){
        Fraction fraction = new Fraction(num, den);
        double b = Fraction.denominator;
        return b;
    }



    public static void main(String[] args) {

       /* Scanner in = new Scanner(System.in); // приенятие дроби в качестве аргумента
          double numerator = in.nextDouble();
          double demomerator = in.nextDouble();
          Fraction NewFraction = new Fraction(numerator, demomerator); */


        /* double a, sA=1;
        double b, sB=1;

        a=multa(10, 3); //первая дробь
        b=multb(10, 3);

        sA=a*sA;
        sB=b*sB;

        a=multa(11, 3); //вторая дробь
        b=multb(11, 3);

        sA=sA*a;
        sB=sA*b;

        System.out.println(sA+"/"+sB);

         */

        //сложение сделать не полуичлось
        }
    }
